# Dependencies
node_modules/
.pnp
.pnp.js
.yarn/install-state.gz

# Build outputs
dist/
out/
dist-electron/
release/
*.exe
*.msi
*.zip
!plugins-dist/.gitkeep
plugins-dist/*
!plugins-dist/.gitkeep

# Cache
.vite/
.cache/
.parcel-cache/
.eslintcache
.stylelintcache
.turbo/

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# IDE / Editor
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# Env & secrets
.env
.env.local
.env.*.local
*.pem
*.key

# Test / coverage
coverage/
*.lcov
.nyc_output/

# Spec Mode artifacts (ignored by git but retained locally)
# .trae/specs/ should be committed (spec mode artifacts)

# Dev-only local data (sqlite DB + logs + uncompressed plugin staging)
.data/
.sqlite/
wal/
*.sqlite-shm
*.sqlite-wal
fmb.db*

# Intermediate build artifacts from esbuild (main process CJS + sourcemaps)
build/
bootstrap/
electron-vite-build.log
boot-markers.log
scripts/verify_task2.cjs

# electron-builder output (NSIS + portable + win-unpacked) — moved off dist/ to
# bypass app.asar lock during iterative repackaging. Never commit installers.
dist2/

# pnpm/npm download cache
.npm-cache/
